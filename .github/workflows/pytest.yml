name: run pytests

on:
  push:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: install pytest
        run: pip install pytest

      - name: run tests
        run: pytest
